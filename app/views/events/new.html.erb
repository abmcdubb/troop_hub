<%=render partial: 'layouts/banner', locals: {:name => "Add Event", :left_option => "Browse events by category and add them to your troop calendar", :right_option => "Search for badge-earning activities" }%>
<br/>
<br/>

<%= render 'form' %>