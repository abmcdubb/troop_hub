

<%= form_tag("/events/search", method: "get") do %>
<%= label_tag(:'event[name]', "Search by Event Name:") %>
  <%= text_field_tag(:'event[name]') %>
  <button type="submit" class="btn btn-default btn-xs">Search</button>
<% end %>